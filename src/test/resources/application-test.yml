spring:
  jpa:
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

app:
  sync:
    run-on-startup: false
  location-usage:
    retention:
      enabled: false
