spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5433}/${DB_NAME:jejugilmoa}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true

external:
  api:
    tour-api:
      service-key: ${TOUR_API_KEY:1c7ed0916603b7992cb0482c7729fad89e275a365973beb6ec812495dc074273}

jwt:
  secret: ${JWT_SECRET:dev-jwt-secret-9e2f4c8a1d7b6e5f0a3c9d2e8f1b7a6c}

app:
  sync:
    run-on-startup: true

